function require() {
    //引入模块
    var forArr = reruire(arrs);
};

function define(arr) {
    //定义模块
    var arrs = arr.some(function(item) {
        return item > 1
    });
};

var arr = [1, 2, 3, ]